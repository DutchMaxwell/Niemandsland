# Table-side parity audit D — 2026-09-08

Today's merged core ports and fixes (#808 Sturdy, #810 Surprise Attack with the review's
F1/D1 fix, #803 Reinforcement round-start part 3, #809 Fear in the melee comparison) and
the #812/#813 demotion: does the TABLE play the same clause? Evidence, not opinion — every
verdict cites `file:line` of a live handler on both sides. The census proves the core reads
a name; only a table audit proves the table plays it.

Method per name: (1) the merged PR body / the core read for the primitive; (2) the table
handler; (3) a verdict. Aura variants must land the base name by NAME.

## Verdict tally

RESOLVES 4 · GAP 1 — **one fix PR: #819 (Surprise Attack burst), RED+GREEN on the box.**

| # | Name | Port/fix PR | Verdict | Table evidence | Core evidence | What a fix would need |
|---|------|-------------|---------|----------------|---------------|----------------------|
| 1 | Sturdy | #808 | RESOLVES | `scripts/main.gd:4663-4674` `_solo_over9_defense_rule`: "Guarded" literal, then the DATA-alias loop `unit_rules_of_primitive(target, "Guarded")` — "Sturdy" resolves by the generic primitive layer, gated on `_solo_rule_on_all_models` (the "all models" quantifier). Consumed at the save seams `main.gd:3115` / `:5611` / `:5988` / `:6340` (the charge arm's `charge_from_in > LONG_RANGE_IN` = over 9") / `:6781` / `:9889`. Registry entry `assets/solo/rules_mechanics_aofr.json:2678-2684` (`defense_bonus: 1`, `over_in: 9`); the aura `Sturdy Boost Aura` grants the base name ("grants", aofr:2696-2702), so the name fallback sees it. Full verdict file: the #808 self-disclosed research note, now written (private `analysis/wave4_unclear/sturdy.md`). | `core/nml-core/src/unit.rs:1953-1964` (epoch-7-gated, all-models → guarded ctx); "Sturdy Boost" as `ShieldedAlias::SturdyBoost` (`unit.rs:51`, `sim.rs:2211`); the over-9" gate pinned by `tests/unit/aura_channel.rs:33-43` (guarded at 7", not at 6"). | nothing. |
| 2 | Surprise Attack | #810 | GAP → fix #819 | The claim arm is live: `scripts/solo/solo_controller.gd:9861-9865` `_reserve_min_enemy_dist_m` — the alias counts and the alias entry's OWN `min_enemy_dist_in` wins (aofr Surprise Attack: 1"); the claim itself `:9871-9876` `_carries_infiltrate_alias` via `RulesRegistry.params_claim_ambush` (the #761 arm). But the BURST ("the first time this unit is activated … within 6\" in LOS, roll X dice, each 2+ one hit AP(1)") had NO table handler at all — "burst" appears nowhere under `scripts/` in a rules sense. **Fix PR #819**: static `surprise_attack_pick` (the Storm Attack descending pick, `unit.rs:2762`) + `_solo_apply_surprise_attack` in the before-attacking slot next to Storm Attack (`main.gd:1074`), params off the registry entry (range_in 6 / trigger 2+ / AP 1), LOS + range filter, `_solo_save_batch` at AP(1), rules-must-log line. RED (job `w3_20260908T072333Z_1474919_da96f6`, the new test's parse-fail on the missing seam) → GREEN (job `w3_20260908T075035Z_1506738_da96f6`: 2819 cases / 0 failures). | `core/nml-core/src/unit.rs:2758-2795` `surprise_attack_of` (BY NAME, epoch-7-gated, rating = the X, bare = 1) + `sim.rs:621`/`:4352` `tray_surprise_attack`; the alias arm `unit.rs:4630-4641` (`min_enemy_dist_in` wins, aofr: 1"). | RESOLVED by #819 (open at audit time). |
| 3 | Reinforcement round-start | #803 | RESOLVES | `scripts/main.gd:10194-10212` `_solo_round_start`, AWAITED before any activation: Battleborn recovery (`:10200`) → Ambush arrivals from round 2 (`:10203-10204`) → `_reinforcement_arrivals` (`:10209`, AFTER the Ambushers, exactly as the rule orders) → `_solo_reinforcement_ai_offers` (`:10212`, withdrawals last — a copy that just returned is never re-offered). The MP/hotseat twin keeps the same order (`main.gd:2001-2014`, "keep this list in lockstep"). | `core/nml-core/src/rollout.rs:610-639` `reinforcement_round_start`: the doc comment names the table beat as the ONLY round boundary and the order ARRIVALS first (:613-614), WITHDRAWALS after (:615-617); `:637-638` calls `reinforcement_arrivals` then `reinforcement_withdrawals`. | nothing — same order, same "after the Ambushers" timing. |
| 4 | Fear(X) in the melee-won comparison | #809 | RESOLVES | `scripts/solo/battle_sim.gd:1456-1462` `_expected_melee_won`: each side's own Fear(X) read via `AiEv.unit_rating(…, "Fear")` rides `AiCombatMath.fear_adjusted_wounds` (`ai_combat_math.gd:338`) into the comparison only — wounds actually removed never change. #809's body: the same helper the table path already uses (`main.gd:8168`, `:10115`). | `core/nml-core/src/sim.rs:2797-2815`: the melee-morale comparison adds `ctx.fear` to each side's dealt tally (`score_su/score_tu`), `fear` stamped at `unit.rs:85` — the identical fold (+X to the comparison only, never to applied wounds). | nothing — both sides count Fear the same way; no divergence. |
| 5 | #812 demotion — the table rule, for the reviewer's comparison with `fix-812-core` | #813 (table half; core half in flight on `fix-812-core`) | NOT-A-GAP (informational) | PR #813, 22 lines in `scripts/solo/solo_controller.gd` `_act`: after every overlay and the planner adoption, before the move executes — if the action is RUSH, no Quick Shot, a ranged weapon exists, the corridor is difficult-capped (the pure `reach_capped_by_difficult` probe), the cap <= the Advance distance (Slow/Fast aware), and a target would be in range + LOS after the capped move → demote to ADVANCE and shoot. Decision record names the rule: `solo_controller.gd:2251` ("GF v3.5.1 p.7: a rush capped to the advance distance forfeits the shot for nothing — chose advances (demoted)"); `:2249` the why line. Slow units keep the rush (their Advance is shorter than the cap); Quick Shot units keep it (they may shoot after it). | `fix-812-core` (in flight) must reproduce the same four conditions and the same Slow/Quick-Shot exemptions on the rollout side; the decision record string above is the comparison key. | nothing on the table side. |

## Notes

- Rows 3 and 4 are the two places where one could most expect drift (an ordering and a
  comparison fold); both are byte-level identical folds on the two sides — the #803 driver
  cites the table beat as its own source of truth, and #809's sim change matches the core's
  existing `ctx.fear` fold.
- The audit-C exclusion ("Sturdy and Surprise Attack: not audited here") is discharged by
  rows 1-2.
